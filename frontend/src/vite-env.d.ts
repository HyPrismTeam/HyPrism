/// <reference types="vite/client" />

// API access should be done via imports from @/api/backend and @/api/bridge avoiding direct usage of window.external or legacy window.go/window.runtime objects
